# Food Manager

Веб-приложение для планирования меню на неделю с автоматическим формированием списка покупок.

## Возможности

- **Каталог продуктов** — база продуктов с пищевой ценностью и информацией об аллергенах
- **Рецепты** — создание и редактирование рецептов с ингредиентами и пошаговыми инструкциями
- **Планирование меню** — выбор блюд на каждый день недели
- **Список покупок** — автоматическое формирование списка с суммированием количеств
- **Учёт калорий** — отслеживание дневной и недельной калорийности
- **Аллергии** — исключение продуктов с аллергенами пользователя

## Технологии

| Компонент | Технология |
|-----------|------------|
| Frontend | Next.js 16, React 19 |
| Стили | Tailwind CSS v4, shadcn/ui |
| База данных | SQLite (Prisma ORM) |
| Авторизация | NextAuth.js |
| Валидация | Zod |

## Установка

```bash
# Клонирование репозитория
git clone <repo-url>
cd food-manager

# Установка зависимостей
npm install

# Настройка базы данных
npx prisma generate
npx prisma db push

# Запуск в режиме разработки
npm run dev
```

## Переменные окружения

Создайте файл `.env`:

```env
DATABASE_URL="file:./dev.db"
NEXTAUTH_SECRET="your-secret-key"
NEXTAUTH_URL="http://localhost:3000"
```

## Структура проекта

```
src/
├── app/                    # Next.js App Router
│   ├── api/               # API endpoints
│   │   ├── products/      # CRUD продуктов
│   │   ├── recipes/       # CRUD рецептов
│   │   ├── meal-plans/    # Планы питания
│   │   └── users/         # Профиль и аллергии
│   ├── products/          # Страницы продуктов
│   ├── recipes/           # Страницы рецептов
│   ├── meal-plan/         # Планирование меню
│   ├── shopping-list/     # Список покупок
│   └── settings/          # Настройки пользователя
├── components/            # React компоненты
│   ├── ui/               # shadcn/ui компоненты
│   ├── layout/           # Header, навигация
│   ├── recipes/          # Компоненты рецептов
│   └── meal-plan/        # Компоненты плана питания
└── lib/                  # Утилиты и конфигурация
    ├── db.ts             # Prisma клиент
    └── auth.ts           # NextAuth конфигурация
```

## Команды

```bash
npm run dev      # Запуск dev сервера
npm run build    # Сборка для production
npm run lint     # Проверка кода
npx prisma studio # GUI для базы данных
```

## Скриншоты

### Планирование меню
Выбирайте рецепты для каждого приёма пищи с отслеживанием калорийности.

### Список покупок
Автоматически формируемый список с группировкой по категориям и исключением аллергенов.

## Лицензия

MIT
